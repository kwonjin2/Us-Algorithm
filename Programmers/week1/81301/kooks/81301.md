# [81301] 숫자 문자열과 영단어

```java
import java.util.*;

class Solution {
    public static Map<String, Integer> numberMap = new HashMap<>();
    static {
        numberMap.put("zero", 0);
        numberMap.put("one", 1);
        numberMap.put("two", 2);
        numberMap.put("three", 3);
        numberMap.put("four", 4);
        numberMap.put("five", 5);
        numberMap.put("six", 6);
        numberMap.put("seven", 7);
        numberMap.put("eight", 8);
        numberMap.put("nine", 9);
    }
    
    public int solution(String s) {
        int answer = 0;
        String word= "";
        for(char ch : s.toCharArray()){
            if(Character.isDigit(ch)){
                answer = answer * 10 + Integer.parseInt(ch + "");
            }else{
                word += ch;
                if(numberMap.containsKey(word)){
                    answer = answer * 10 + numberMap.get(word);
                    word ="";
                }
            }
        }
        
        return answer;
    }
}
```

```python
def solution(s):
    answer = 0
    number_dict ={"zero":0, "one":1, "two":2, "three":3, "four":4, "five":5, "six":6, "seven":7, "eight":8, "nine":9}
    
    word = ""
    for alphaber in s:
        if alphaber.isalpha():
            word += alphaber
            if word in number_dict:
                answer = (answer * 10) + number_dict[word]
                word = ""
        else:
            answer = (answer * 10) + int(alphaber)
                
    
    return answer

# 1478 → "one4seveneight" 주어졌을 때 
# one, 4, seven, eight 
# 숫자면 따로 뺴서 패스
# 글자를 추출해서 숫자단어를 만들고 비교에서 출력


```